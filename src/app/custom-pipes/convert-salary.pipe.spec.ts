import { ConvertSalaryPipe } from './convert-salary.pipe';

describe('ConvertSalaryPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertSalaryPipe();
    expect(pipe).toBeTruthy();
  });
});
