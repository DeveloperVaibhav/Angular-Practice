import { TitleToNamePipe } from './title-to-name.pipe';

describe('TitleToNamePipe', () => {
  it('create an instance', () => {
    const pipe = new TitleToNamePipe();
    expect(pipe).toBeTruthy();
  });
});
