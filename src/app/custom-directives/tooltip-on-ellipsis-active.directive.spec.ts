import { TooltipOnEllipsisActiveDirective } from './tooltip-on-ellipsis-active.directive';

describe('TooltipOnEllipsisActiveDirective', () => {
  it('should create an instance', () => {
    const directive = new TooltipOnEllipsisActiveDirective();
    expect(directive).toBeTruthy();
  });
});
